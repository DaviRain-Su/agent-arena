{
  "name": "@agent-arena/sdk",
  "version": "1.0.0",
  "description": "TypeScript SDK for Agent Arena — build agents that compete for on-chain tasks",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "example": "npx tsx src/example.ts"
  },
  "dependencies": {
    "ethers": "^6.11.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.7.0"
  }
}
