name = "agent-arena-indexer"
main = "src/index.ts"
compatibility_date = "2024-04-01"
compatibility_flags = ["nodejs_compat"]

# ─── D1 Database ──────────────────────────────────────────────────────────────
[[d1_databases]]
binding = "DB"
database_name = "arena-db"
database_id = "21a7ddd3-b104-45b7-a598-fce03294f7ef"   # wrangler d1 create arena-db

# ─── Cron: sync chain events every minute ────────────────────────────────────
[triggers]
crons = ["*/1 * * * *"]

# ─── Environment Variables ────────────────────────────────────────────────────
# Set secrets via: wrangler secret put CONTRACT_ADDRESS
# Or use [vars] for non-sensitive values

[vars]
CONTRACT_ADDRESS = "0x964441A7f7B7E74291C05e66cb98C462c4599381"
XLAYER_RPC = "https://rpc.xlayer.tech"
XLAYER_RPC_FALLBACK = "https://xlayer-mainnet.g.alchemy.com/v2/m2tKWaM3AGwrMPEaOKaEx"
SYNC_BATCH_SIZE = "50"    # blocks per eth_getLogs call (official RPC supports 100)
# x402 premium API: OKB payments are sent to this address
PAYMENT_RECIPIENT = "0xE18756E756f0F471FA3f9559a22334a1be8D9bc9"

# For production mainnet:
# XLAYER_RPC = "https://xlayer-mainnet.g.alchemy.com/v2/m2tKWaM3AGwrMPEaOKaEx"

# ─── Local dev ────────────────────────────────────────────────────────────────
[dev]
port = 3001
local_protocol = "http"
